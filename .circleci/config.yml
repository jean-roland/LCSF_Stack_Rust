version: 2.1

jobs:
  run-ci:
    docker:
      - image: cimg/rust:1.85.0
    steps:
      - checkout

      # Standard Rust Pipeline
      - run: cargo build
      - run: cargo test
      - run: cargo fmt --check

workflows:
  run-ci-workflow:
    jobs:
      - run-ci
